package Entry;

import java.io.File;
public class GUI extends Entry {
    public GUI(File userFile, File softFile){
        super(userFile,softFile);
    }
    @Override
    public Entry run() {
        return null;
    }
}
